update_component:
  name: Actualizar componente desde GitHub
  description: Elimina la versión anterior de una carpeta en custom_components y descarga la última desde GitHub.
  fields:
    folder_name:
      name: Nombre de la carpeta
      description: Nombre del componente/carpeta a actualizar (ej. service, configred).
      required: true
      example: "service"
      selector:
        text: